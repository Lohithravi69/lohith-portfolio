/* Disabled: install-inter.js

This file was disabled by automation because the project switched to a system-first font stack and
we don't want the helper to fetch Google Fonts automatically in CI.

Original script archived here for reference.
*/

// Original script archived in repository metadata. If you need to re-enable, review contents and
// rename back to install-inter.js after auditing.
